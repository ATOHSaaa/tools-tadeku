import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const dataPath = path.join(__dirname, '../shared/etude-data.js');

const ORIGINAL_PROMPTS = [
  '傘を忘れた帰り道', '最後の電車', '誰もいない屋上', '雨のバス停', '古い手紙が見つかった',
  '隣の部屋の物音', '真夜中のコンビニ', '消えた一日', '借り物のコート', '待ち合わせの相手が来ない',
  '鏡の向こう', '駅のホームで', '白いハンカチ', '閉店した映画館', '灯台の見える丘',
  '記憶を失った朝', '二度目の別れ', '誤送信', '最後の電話', '約束の破り方',
  '口のきけない歌手', '名もなき画家', '引退した刑事', '見知らぬ女からの手紙', '幽霊との同居',
  '密偵の午後', '旅館の夜', '廃墟の図書館', '桟橋の夕暮れ', '雪の駅',
  '地下鉄の終点', '橋の上で', '森の奥の小屋', '港の倉庫', '屋根裏部屋',
  '灯籠の下', '縁側の午後', '竹林の小道', '鳥居の前', '石段を上る',
  '風鈴の音', '障子の向こう', '銭湯の帰り', '回転寿司のカウンター', '骨董市の一角',
  '古着屋の試着室', '駄菓子屋の前', '天文台の夜', '水族館の閉館後', '遊園地の廃墟',
  '無人駅のプラットホーム', '国境の検問所', '空港の待合室', '地下街の迷子', '避難所の朝',
  '煙突の見える町', '廃校の教室', '干上がった川床', '潮騒の聞こえる崖', '蜃気楼',
  '極光の下で', '流星群の夜', '初雪の朝', '春一番', '麦の秋',
  '厳冬の旅', '真夏の午後', '夕焼けの屋上', '朝霧の中', '台風の前日',
  '雷鳴のあと', '虹がかかった', '花火の余韻', '満月の夜', '蝉の声が止んだ',
  '金縛りにあう', '予知夢', 'デジャヴュ', '別世界への扉', '結界の内側',
  '禁断の本', '契約書に署名する', '呪いの人形', '祈りの言葉', 'タイムマシンの故障',
  'UFOの着陸', 'ロボットの告白', '魔法の代償', '予言の実現', 'サイバーパンクの路地',
  'スチームパンクの工房', '仮想世界のログアウト', '100年後の手紙', '100年前の約束', '繰り返す一日',
  '止まった秒針', '逆さの世界', '地図にない場所', '名前のない駅', '行き止まり',
  '環状線の終点', '片道切符', '乗り遅れた朝', '始発の車両', '終電を逃した',
  '隣の席の乗客', '改札の向こう', '居眠りの旅', '落とし物の届け出', '間違い電話',
  '既読スルー', '留守番電話', '誤爆のメッセージ', 'グループチャットから抜ける', '最後の顧客',
  '閉店セール', '開店前の店', '臨時休業の看板', '取り壊しの前日', 'コーヒーが冷めた',
  '味噌汁の湯気', '焼き鳥の店の閉店', '缶コーヒーの苦さ', '高級レストランの一人席', '屋台のラーメン',
  '弁当のおかずだけ', '酒の残り', '結婚式の控室', '葬式のあと', '入学式の朝',
  '引っ越しの段ボール', '就職の初日', '退職の日', '誕生日の夜', 'クリスマスの一人',
  'お盆の帰省', '初恋の再会', '片思いの告白', '失恋の翌朝', '復縁の提案',
  '婚約の破談', '離婚届', '再婚の決意', '幼なじみとの再会', '元恋人の結婚式',
  '恩人への手紙', '仇との対峙', 'ライバルの敗北', '見知らぬ助言者', '隣人の秘密',
  '大家さんの訪問', '探偵の依頼', '記者の取材', '弁護士の相談', '教師の放課後',
  '科学者の実験', '詩人の原稿', '大道芸の後', '占い師の予言', '王の退位',
  '姫の逃亡', '騎士の誓い', '盗賊の良心', '傭兵の報酬', '追放された王子',
  '亡命者の夜', '宇宙飛行士の日記', '潜水艦の窓', '灯台守の交代', '南極の基地',
  '砂漠のオアシス', '最後の一冊', '消えた言葉', '忘れられた歌', '読めない文字',
  '逆さの地図', '赤い糸', '切れた糸', '結び目', '鍵穴の向こう',
  '隙間から見える', 'しおりのページ', '折り目の跡', '笑い声だけが残る', '足音が遠ざかる',
  '物音が止む', '沈黙のあいだ', 'ささやき声', '叫びのあと', 'ため息',
  '香水の残り香', '煙草の火', '雨の匂い', '海の匂い', '古い本の匂い',
  '指紋のついたガラス', '傷跡', '涙の跡', '賭けの結果', '勝負の行方',
  '反則の判定', 'タイムアップ', '延長戦', '合図の意味', '暗号の解読',
  '合言葉', 'パスワード', '偽の身分', '変装', '仮面の下',
  '祭りの後片付け', '花見の夜', '紅葉狩りの帰り', '盆踊りの輪', '運動会の閉会式',
  '文化祭の撤収', '試験の翌日', '合格発表の朝', '遺失物の引き取り', '落とし主の正体',
  '拾得者の選択', '保管期限', '引きこもりの窓', '転職の面接', '定年の挨拶',
  '一人暮らしの初日', 'シェアハウスのキッチン', 'カプセルホテルの夜', '実家の居間', '引っ越し先の荷解き',
  '偶然の一致', '必然の出会い', 'すれ違い', '遅れてきた手紙', '最後のチャンス',
  '取り返しのつかない選択', '間違った電話番号', '間違った住所', '間違った人', '間違った荷物',
  '本物の偽物', '嘘の真実', '見せかけの平和', '静かな革命', '小さな奇跡',
  '大きな誤解', '些細な親切', '重い一言', '破られた約束', '守られた約束',
  '忘れられた約束', '消えた町', '現れた町', '待ち人', '来ない人',
  '来るはずのない人', '最後の客', '常連の席', '古い約束', '口約束',
  '血の誓い', '消えた声', '戻った声', '他人の声', '亡き人の声',
  '見えない友達', '人間のふりをする何か', '動く写真', '喋る絵', '歌う時計',
  '泣く人形', '笑う鏡', '最後の花火', '二度目の春', '終わらない夏',
  '短い秋', '長い冬', '暖かい雨', '冷たい太陽', '明るい夜',
  '暗い昼', '逆走', '逆行', '逆転', '白紙の手紙',
  '長すぎる手紙', '届かなかった手紙', '返事のない手紙', '最後の一歩', '踏み出せない一歩',
  '戻れない一歩', '決意の一歩', 'まにまに', '徒然', '異邦人',
  '陽炎', '薄氷', '面影', '凌霄', '紙の月',
  'やまない雨', '見た夢', '星の海', '人の営み', '夜の底',
  '海の底', '光の中', 'ゆりかご', '魚群', '宅配ピザ',
  '貸し出しノート', '万年筆のインク', '蝋燭の炎', '封蝋の印', '軍手の汚れ',
  '安全ピン', '紐靴のひも', '帆布鞄の中身', '鍵束', '枯れ木の下',
  '暗証番号', '口伝の秘密', '掲示板の欄外', '書評の一行', '朗読会の後',
  '受話器を置く', 'フィルムの現像', '暗室の赤い光', '遺言の開封', '宝の地図',
  '暗号メッセージ', '偽物の発見', '贋作の真実', '病院の夜', '裁判の判決',
  '警察署の取調室', '学校の屋上', '会社の残業', '工場の夜勤', '寺の境内',
  '神社の手水舎', '墓地の夕方', '刑務所の面会', '断崖の縁', '温泉の露天',
  '田舎のバス', '大都市の迷路', '100万円の行方', '借金の督促', '宝くじの番号',
  '遺産の分配', '給料日の夜', 'コイン一枚', '空白の小切手', '黒猫の横切り',
  '鳩の群れ', 'カラスの鳴き声', '金魚の水槽', '蛇の脱皮', '蝶の羽',
  '狼の遠吠え', '亀の歩み', 'ピアノの調律', 'ギターの弦', 'ラジオの雑音',
  'カセットテープ', 'レコードの針', '未完成の作品', '迷宮の入口', '秘密の部屋',
  '隠し扉', '地下通路', '屋上の庭', '電話ボックス', '公衆電話',
  'ポストの中', '自動販売機の前', 'エレベーターの停止', '赤信号のあいだ', '渋滞の車内',
  'フェリーの甲板', '飛行機の窓', '自転車の鍵', '深夜バスの最後尾', '看護師の夜勤',
  '消防士の出動', '漁師の朝', '農家の収穫', '店員の閉店作業', 'バーテンダーの一杯',
  '運転手の休憩', '清掃員の朝', '後悔の念', '未練', '執着',
  '諦め', '受容', '解放', '赦し', '裏切り',
  '偶然', '運命', '正体', '記憶', '時間',
  '名前', '匂い', '影', '夢',
];

const times = [
  '真夜中', '明け方', '夜明け', '午前零時', '正午', '夕暮れ', '黄昏', '宵闇', '未明', '薄明',
  '真昼', '午後', '深夜', '早朝', '日没', '日の出', '宵', '丑三つ時', '逢魔が時', '子刻',
  '梅雨', '真夏', '真冬', '初秋', '晩秋', '春先', '残暑', '小春', '霜降り', '雪解け',
];

const places = [
  '屋上', '地下室', '屋根裏', '廊下', '階段', 'エレベーター', '駐車場', '路地', '交差点', '踏切',
  'ホーム', '改札', '車内', '船倉', '甲板', '空港', '待合室', 'ロビー', '食堂', '厨房',
  '教室', '職員室', '保健室', '図書室', '実験室', '屋上庭園', '校庭', 'プール', '体育館', '音楽室',
  '病室', '待合', '手術室', '診察室', '屋上テラス', '屋敷', '離れ', '茶室', '書斎', '台所',
  '居間', '寝室', '納戸', '物置', '倉庫', '工場', '作業場', '事務所', '応接室', '喫茶店',
  'バー', '居酒屋', '屋台', '市場', '商店街', 'デパート', '書店', '花屋', '港', '桟橋',
  '灯台', '断崖', '砂浜', '岩場', '入り江', '岬', '浜辺', '漁港', '森', '林道',
  '渓谷', '滝', '湖畔', '沼', '湿地', '草原', '高原', '峠', '洞窟', '坑道',
  '廃坑', '廃墟', '廃屋', '廃校', '廃病院', '廃寺', '廃駅', '廃工場', '神社', '境内',
  '本殿', '鳥居', '墓地', '納骨堂', '教会', '礼拝堂', '修道院', '鐘楼', '城', '城壁',
  '天守', '地下牢', '闘技場', '劇場', '楽屋', '舞台裏', '美術館', '画廊', '天文台', '観測所',
  '研究所', '資料室', 'アーカイブ', '地下書庫', '閉鎖区域', '検問所', '国境', '関所', 'シェルター', '避難所',
];

const moments = [
  '別れ', '再会', '告白', '謝罪', '別れ際', '最後の夜', '最初の朝', '決断', '後悔', '赦し',
  '約束', '嘘', '真実', '秘密', '暴露', '沈黙', '別れ話', '口論', '和解', '出発',
  '到着', '帰還', '逃亡', '追跡', '潜伏', '待機', '観察', '尾行', '潜入', '目覚め',
  '眠り', '夢', '幻', '記憶', '忘却', '回想', '予感', '確信', '疑念', '勝利',
  '敗北', '引退', '復帰', '転身', '覚醒', '堕落', '再生', '崩壊', '再建', '開店',
  '閉店', '開演', '閉幕', '開票', '解散', '結成', '独立', '合併', '破産', '誕生',
  '死', '臨終', '葬儀', '法事', '供養', '祈り', '呪い', '祝福', '呪詛', '別離',
];

const occupations = [
  '医者', '看護師', '教師', '学生', '弁護士', '警察官', '探偵', '記者', '編集者', '作家',
  '画家', '音楽家', '俳優', '舞者', '職人', '農夫', '漁師', '船長', 'パイロット', '運転手',
  '店員', 'バーテンダー', 'シェフ', '清掃員', '警備員', '消防士', '救急隊', '司書', '考古学者', '科学者',
  '技師', '発明家', '錬金術師', '占い師', '僧侶', '神官', '巫女', '騎士', '傭兵', '王',
  '女王', '王子', '姫', '大臣', '密偵', '盗賊', '詐欺師', '賭博師', '旅人', '亡命者',
  '難民', '移民', '帰郷者', '放浪者', '隠者', '世捨て人', '番人', '灯台守', '駅員', '宇宙飛行士',
  '潜水士', '登山家', '探検家', '地図製作者', '翻訳者', '通訳', '外交官', '間諜', '暗殺者', '詩人',
];

const objects = [
  '手紙', '封筒', 'はがき', '日記', '原稿', '写真', 'フィルム', 'カメラ', '録音', 'テープ',
  '鍵', '鍵束', '南京錠', '暗証番号', '合鍵', '懐中時計', '腕時計', '砂時計', '目覚まし', '停止した時計',
  '鏡', '眼鏡', '双眼鏡', '望遠鏡', '虫眼鏡', '地図', '羅針盤', 'コンパス', '方位磁石', '星図',
  '傘', 'コート', '帽子', '手袋', 'スカーフ', 'ハンカチ', '指輪', 'ネックレス', 'ブローチ', '腕輪',
  '刀', '短刀', '銃', '弾丸', '薬', '毒', '解毒剤', '注射器', '包帯', '血痕',
  '花', '花束', '枯れ花', '種', '苗', '盆栽', '花びら', '落ち葉', '枝', '実',
  '本', '古書', '禁書', '白紙の本', '読めない本', '日記帳', '帳簿', '契約書', '遺言', '証明書',
  '箱', '桐箱', '宝箱', '空箱', '重い箱', '小箱', '包み', '荷物', 'トランク', 'スーツケース',
  '瓶', '空瓶', '薬瓶', 'ランプ', '蝋燭', '灯火', '提灯', '電球', '懐中電灯', 'マッチ',
  'コップ', '茶碗', '皿', '箸', 'スプーン', 'ナイフ', 'フォーク', '鍋', 'やかん', '酒瓶',
  '硬貨', '紙幣', '小切手', '借金証', '領収書', '切符', '定期券', '名刺', '身分証', 'パスポート',
  '人形', 'ぬいぐるみ', '操り人形', '置物', '彫像', '絵画', '額縁', 'スケッチ', '草稿', '未完成品',
  '楽器', 'ピアノ', 'バイオリン', 'ギター', 'フルート', 'ドラム', 'ハープ', 'オルガン', '笛', '琴',
  '車', '自転車', 'ボート', '筏', '馬車', '列車', '飛行機', '気球', '潜水艦', '宇宙船',
];

const adjectives = [
  '古い', '新しい', '壊れた', '空の', '重い', '軽い', '濡れた', '乾いた', '冷たい', '温かい',
  '赤い', '青い', '白い', '黒い', '金色の', '銀色の', '透明な', '曇った', '割れた', '欠けた',
  '忘れられた', '見捨てられた', '封印された', '禁じられた', '失われた', '見つかった', '隠された', '盗まれた', '返された', '届いた',
  '最後の', '最初の', '二度目の', '三度目の', '別れの', '再会の', '秘密の', '偽りの', '本物の', '名もなき',
  '遠い', '近い', '高い', '低い', '深い', '浅い', '長い', '短い', '広い', '狭い',
  '静かな', '騒がしい', '暗い', '明るい', '穏やかな', '荒れた', '穢れた', '清らかな', '歪んだ', 'まっすぐな',
];

const verbs = [
  '鳴る', '止む', '消える', '現れる', '開く', '閉じる', '割れる', '落ちる', '浮かぶ', '沈む',
  '届く', '届かない', '返る', '戻る', '去る', '来る', '待つ', '呼ぶ', '答える', '黙る',
  '燃える', '凍る', '溶ける', '乾く', '濡れる', '輝く', '曇る', '揺れる', '止まる', '動く',
  '咲く', '散る', '枯れる', '芽吹く', '実る', '腐る', '生まれる', '死ぬ', '眠る', '目覚める',
];

const subjects = [
  '雨', '雪', '風', '雷', '霧', '霞', '氷', '霜', '露', '虹',
  '月', '星', '太陽', '雲', '影', '光', '闇', '炎', '煙', '埃',
  '鐘', 'サイレン', '警報', '電話', 'ドアベル', '物音', '足音', '息遣い', '心臓', '時計',
  '視線', '涙', '笑み', '傷', '血', '汗', '声', '言葉', '沈黙', '記憶',
];

const concepts = [
  '余白', '余韻', '残響', '残像', '面影', '気配', '郷愁', '憧憬', '懐古', '追憶',
  '寂寥', '孤独', '疎外感', '隔たり', '邂逅', '宿命', '因縁', '因果', '報い', '贖罪',
  '執着', '未練', '後悔', '悔恨', '自責', '罪悪', '安堵', '希望', '絶望', '諦念',
  '覚悟', '決意', '躊躇', '迷い', '彷徨', '逡巡', '望郷', '追想', '回想', '郷思い',
];

const weather = [
  '雨', '雪', '霧', '霞', '風', '嵐', '台風', '雷雨', '霙', '雹',
  '晴天', '曇天', '夕立', '春雨', '秋雨', '初雪', '積雪', '吹雪', '凍雨', '虹',
];

const animals = [
  '猫', '犬', '鳥', '鴉', '鷹', '狐', '鹿', '兎', '熊', '狼',
  '蛇', '亀', '蛙', '蝶', '蜻蛉', '蝉', '蛍', '鯨', '海豚', '鮫',
  '馬', '牛', '羊', '豚', '鶏', '鳩', 'カラス', 'スズメ', 'フクロウ', 'コウモリ',
];

const actions = [
  '扉を開ける', '扉を閉じる', '窓を開ける', '窓を閉じる', 'カーテンを開ける', 'カーテンを閉じる',
  '電話に出る', '電話を切る', '留守番電話を残す', 'メッセージを送る', 'メッセージを消す', '既読をつける',
  '手紙を書く', '手紙を破る', '手紙を焼く', '手紙を封じる', '手紙を開封する', '手紙を投函する',
  '鍵を回す', '鍵をかける', '鍵を開ける', '鍵を失くす', '鍵を拾う', '鍵を返す',
  '荷物をまとめる', '荷物を解く', '荷物を置く', '荷物を持つ', '荷物を忘れる', '荷物を届ける',
  '約束を守る', '約束を破る', '約束を忘れる', '約束を交わす', '約束を撤回する', '約束を思い出す',
  '嘘をつく', '真実を話す', '秘密を守る', '秘密を漏らす', '秘密を聞く', '秘密を知る',
  '逃げる', '追う', '待つ', '去る', '戻る', '立ち止まる',
  '目を閉じる', '目を開ける', '振り返る', '俯く', '仰ぐ', '手を伸ばす',
  '火をつける', '火を消す', '灯りを点ける', '灯りを消す', '蝋燭を吹き消す', 'マッチを擦る',
  'コーヒーを淹れる', 'お茶を注ぐ', '酒を酌む', 'グラスを置く', '杯を交わす', '乾杯する',
  '写真を撮る', '写真を焼く', '写真を破る', '写真を見つめる', '写真を隠す', '写真を飾る',
  '地図を広げる', '地図を折る', '地図を描く', '地図を焼く', '地図を渡す', '地図を読む',
  '時計を見る', '時計を止める', '時計を合わせる', '時計を壊す', '時計を直す', '時計を捨てる',
  '名前を呼ぶ', '名前を忘れる', '名前を聞く', '名前を書く', '名前を消す', '名前を変える',
  '道を歩く', '道を迷う', '道を選ぶ', '道を戻る', '道を進む', '道を渡る',
  '橋を渡る', '橋を渡らない', '川を渡る', '海を渡る', '山を越える', '森を抜ける',
  '星を数える', '雲を眺める', '雨を聞く', '風を感じる', '雪を見る', '月を仰ぐ',
];

const standalone = [
  '遠い記憶', '近い未来', '別の人生', 'もう一つの選択', '取り返しのつかない朝', '二度と来ない夕暮れ',
  '言えなかった一言', '聞けなかった答え', '届かなかった想い', '戻れない場所', '忘れられない顔',
  '見覚えのある景色', '見覚えのない街', '知らない名前', '覚えている声', '忘れた約束',
  '約束の場所', '待ち合わせの時間', '最後の便', '最初の一歩', '踏み出せない一歩',
  '白い壁', '黒い海', '赤い空', '青い炎', '銀の雨', '金の砂',
  '無音の部屋', '無人の島', '無名の駅', '無題の詩', '無言の別れ', '無限の階段',
  '片道の旅', '往復の切符', '終点のない線路', '始点のない道', '地図の端', '世界の果て',
  '時の流れ', '時の停止', '時の逆行', '時の隙間', '時の贈り物', '時の罰',
  '夢の続き', '夢の終わり', '夢の中の夢', '現実の夢', '夢と現実の境界', '夢から覚める',
  '鏡の中の自分', '写真の中の人', '絵の中の世界', '本の中の物語', '窓の向こうの世界', '扉の向こうの光',
  '最後の晩餐', '最初の朝食', '一人の夕食', '二人の昼食', '三人の茶会', '多くの声',
  '小さな嘘', '大きな真実', '軽い約束', '重い誓い', '短い別れ', '長い待ち',
  '静かな怒り', '穏やかな悲しみ', '穏やかな喜び', '激しい平静', '冷たい温もり', '温かい孤独',
  '見えない糸', '切れた糸', '絡まった糸', '赤い糸', '銀の糸', '糸のない操り人形',
  '言葉のない歌', '歌のない言葉', '色のない絵', '絵のない色', '形のない影', '影のない形',
  '呼吸のリズム', '心臓の音', '血管の脈', '神経の疼き', '骨の軋み', '皮膚の記憶',
  '街の灯り', '港の灯り', '山の灯り', '海の灯り', '空の灯り', '星の灯り',
  '冬の手袋', '夏の扇子', '春のコート', '秋のマフラー', '雨の靴', '雪の帽子',
  '古い地図', '新しい地図', '白地の地図', '逆さの地図', '燃える地図', '消えた地図',
  '空の椅子', '空の席', '空の部屋', '空の瓶', '空の箱', '空の手',
  '満ちた月', '欠けた月', '新月', '三日月', '半月', '月食',
  '昇る太陽', '沈む太陽', '止まった太陽', '二つの太陽', '消えた太陽', '偽りの太陽',
  '流れる川', '止まった川', '逆流する川', '涸れた川', '溢れた川', '凍った川',
  '燃える森', '凍った森', '消えた森', '迷いの森', '禁じられた森', '始まりの森',
  '開いた本', '閉じた本', '読めない本', '白紙の本', '血で書かれた本', '消えた本',
  '鳴る電話', '鳴らない電話', '壊れた電話', '赤い電話', '黒い電話', '沈黙の電話',
  '動く写真', '止まった写真', '消えた写真', '偽りの写真', '古い写真', '未来の写真',
  '割れた鏡', '曇った鏡', '二面の鏡', '呪われた鏡', '消えた鏡', '真実の鏡',
  '止まった時計', '逆走する時計', '壊れた時計', '二つの針', '消えた針', '止まらない時計',
  '開いた扉', '閉じた扉', '隠し扉', '消えた扉', '二重の扉', '一方通行の扉',
  '上る階段', '下る階段', '終わりのない階段', '消えた階段', '血の階段', '光の階段',
  '沈む船', '浮かぶ船', '幽霊船', '錆びた船', '小さな船', '大きな船',
  '届かない電波', '途切れた通信', '最後の便', '遅延便', '欠航', '臨時便',
  '消灯の合図', '避難訓練', '火災報知器', '非常口', '非常階段', '避難経路',
  '指紋認証', '顔認証', '暗証入力', '二段階認証', 'ログイン失敗', 'アカウント停止',
  '最終ログ', 'バックアップ', 'データ消失', '復元作業', '初期化', '上書き保存',
  '最終通告', '猶予期間', '期限切れ', '延長申請', '再審請求', '上訴の放棄',
  '遺品整理', '生前整理', '引き継ぎ', '後任の挨拶', '引継書', '引き継ぎ不足',
  '最終面接', '内定通知', '不採用通知', '辞退の連絡', '入社式', '研修の初日',
  '最終試験', '追試', '補習', '留年', '卒業式', '学位授与',
  '最終公演', '千秋楽', 'カーテンコール', 'アンコール', '打ち上げ', '解散式',
  '最終入稿', '校正刷り', '重版決定', '絶版', '復刊', '文庫化',
  '最終調整', 'リハーサル', '本番前夜', '開演五分前', '開演遅延', '途中中断',
  '最終通告状', '内容証明', '調停', '仲裁', '和解案', '執行猶予',
  '最終報告', '中間報告', '進捗遅延', '予算超過', '白紙撤回', 'プロジェクト凍結',
  '最終患者', '当番交代', '夜勤明け', '早番入り', '休日出勤', '代休',
  '最終便', '始発待ち', '乗り継ぎ', '乗り換え', '乗り過ごし', '降り忘れ',
  '最終セール', '閉店ガラガラ', '在庫一掃', '返品期限', '交換不可', 'レシート紛失',
  '最終通告', '門限', '夜遊び', '朝帰り', '鍵の紛失', '合鍵の返却',
  '最終確認', '念のため', '再チェック', '見落とし', '取りこぼし', '手違い',
  '最後の言葉', '最初の言葉', '言い残し', '言えなかった謝罪', '遅すぎた告白', '間に合った一言',
  '最後の顧客', '最初の常連', '新しい顔', '見知らぬ常連', '席替え', 'カウンター越し',
  '最後の一皿', '最初の一口', '残した料理', '食べ残し', 'お代わり', '締めの一杯',
  '最後の一ページ', '最初の一行', 'しおりの位置', '折り返し', '余白の落書き', '欄外のメモ',
  '最後の一画', '最初の一筆', '下書きの跡', '消しゴムの粉', 'インクの滲み', '筆の折れ',
  '最後の一音', '最初の音', '調律前', '弦の切れ', '弓の毛', '譜面のめくれ',
  '最後の一歩', '最初の一歩', '踏み出せない一歩', '戻れない一歩', '迷った分岐', '遠回り',
  '最後の光', '最初の光', '灯りの消滅', '明かりの点滅', '停電', '復電',
  '最後の雨', '最初の雪', '季節のずれ', '気象警報', '避難勧告', '解除',
  '最後の夏', '最初の秋', '短い春', '長い冬', '季節外れ', '記憶の季節',
  '最後の約束', '最初の約束', '口約束', '書面のない約束', '破られた約束', '守られた約束',
  '最後の手紙', '最初の手紙', '返事のない手紙', '届かない手紙', '長すぎる手紙', '短すぎる手紙',
  '最後の電話', '最初の電話', '切れた電話', 'かけ直し', '番号変更', '連絡先削除',
  '最後の会話', '最初の会話', '言葉に詰まる', '沈黙の会話', '別れの会話', '再会の会話',
  '最後の朝', '最初の夜', '眠れない夜', '過ぎた夜', '早すぎる朝', '遅すぎる朝',
  '最後の別れ', '最初の出会い', 'すれ違い', '偶然の再会', '必然の別れ', '運命の出会い',
  '最後の笑顔', '最初の涙', '隠した涙', '堪えた笑み', '作り笑い', '本当の笑顔',
  '最後の記憶', '最初の記憶', '曖昧な記憶', '鮮明な記憶', '偽りの記憶', '共有の記憶',
  '最後の名前', '最初の名前', '仮の名前', '忘れた名前', '変えた名前', '呼ばれない名前',
  '最後の住所', '最初の住所', '仮住まい', '居候', '転々', '定住',
  '最後の仕事', '最初の仕事', '副業', '兼業', '無職', '再就職',
  '最後の旅', '最初の旅', '一人旅', '帰らぬ旅', '短い旅', '長い旅',
  '最後の歌', '最初の歌', '忘れた歌', '覚えた歌', '口ずさみ', '合唱',
  '最後の舞', '最初の舞', '踊れない夜', '踊り明かす', '音楽のない舞', '一人の舞',
  '最後の絵', '最初の絵', '描けない日', '描きかけ', '額に入れた絵', '捨てた絵',
  '最後の詩', '最初の詩', '韻を踏む', '自由詩', '朗読', '黙読',
  '最後の物語', '最初の物語', '語り継ぐ', '語らない', '語り終える', '語り始める',
];

const objectPrefixes = [
  '最後の', '消えた', '戻らない', '二度目の', '見知らぬ', '古い', '空白の', '止まった',
  '忘れられた', '封印された', '届かない', '失われた', '隠された', '見つかった', '壊れた', '空の',
];

const placeSuffixes = ['で', 'から', 'へ', 'の向こう', 'の奥', 'の隅', 'の影', 'の底', 'の上', 'の前'];

const placeQualities = [
  '空気', '匂い', '温度', '湿度', '光', '闇', '音', '静けさ', '景色', '記憶',
  '壁', '床', '窓', '扉', '屋根', '階段', '天井', '角落', '入口', '出口',
  '朝日', '夕日', '月明かり', '灯り', '影', '風', '雨音', '残響', '気配', '余白',
];

const bodyParts = [
  '手', '目', '声', '足', '背中', '肩', '頬', '髪', '指', '喉',
  '瞳', '爪', '歯', '唇', '耳', '額', '首', '胸', '腹', '魂',
];

const leftovers = [
  'だけ', 'ばかり', 'しかない', 'の跡', 'のかけら', 'の欠片', 'の断片', 'の残り',
  'のはじまり', 'の終わり', 'の合図', 'の予感', 'の気配', 'の輪郭', 'の断面',
];

const microScenes = [
  '一人きり', '二人だけ', '誰もいない', '気配だけ', '声だけ', '影だけ', '灯だけ',
  '雨の音', '風の音', '波の音', '鐘の音', '足音だけ', '呼吸だけ', '涙だけ',
  '指先だけ', '背中だけ', '視線だけ', '笑みだけ', '沈黙だけ', '残像だけ',
  '名もなき', '色もなき', '形もなき', '音もなき', '匂いだけ', '温もりだけ',
  '冷たさだけ', '重さだけ', '軽さだけ', '遠さだけ', '近さだけ', '長さだけ',
];

const placeVerbs = [
  'に残る', 'に眠る', 'に響く', 'に佇む', 'を後に', 'を見下ろす', 'を見上げる',
  'で過ごす', 'で待つ', 'から逃げる', 'へ向かう', 'を出る', 'を離れる', 'に落ちる',
];

const seasonTouches = [
  '春の風', '夏の暑さ', '秋の深み', '冬の冷え', '梅雨の湿気', '初雪の白', '残暑の厳しさ',
  '木漏れ日', '夕立の後', '台風一過', '花冷え', '雪解け水', '蝉の声', '紅葉の色',
];

const ENDING_WORDS = [
  ...objects,
  ...places,
  ...bodyParts,
  ...animals,
  ...occupations,
  ...moments,
  ...concepts,
  ...verbs,
  ...subjects,
  ...weather,
  ...times,
  ...placeQualities,
  ...seasonTouches,
  ...microScenes,
  ...actions,
  ...standalone,
].sort((a, b) => b.length - a.length);

const MAX_ENDING_COUNT = 2;
const LIMITED_PLACES = new Set(['屋上', '地下室']);
const MAX_LIMITED_PLACE_COUNT = 10;

function limitedPlaceHits(prompt) {
  const hits = [];
  for (const place of LIMITED_PLACES) {
    const prefix = new RegExp(`^${place}(の|に|を|へ|で|から)`);
    if (prefix.test(prompt)) hits.push(place);
    else if (prompt.endsWith(`の${place}`)) hits.push(place);
  }
  return hits;
}

function endingKey(prompt) {
  const particle = prompt.match(/(で|から|へ|まで|より)$/);
  if (particle) return particle[1];
  const tailWord = prompt.match(/(ばかり|しかない|だけ)$/);
  if (tailWord) return tailWord[1];
  const noMatch = prompt.match(/の([^の]+)$/);
  if (noMatch) return noMatch[1];
  for (const word of ENDING_WORDS) {
    if (prompt.endsWith(word)) return word;
  }
  const gaMatch = prompt.match(/が([^が]+)$/);
  if (gaMatch) return gaMatch[1];
  const woMatch = prompt.match(/を([^を]+)$/);
  if (woMatch) return woMatch[1];
  if (prompt.length <= 4) return prompt;
  return prompt.slice(-2);
}

function isValidPrompt(prompt) {
  if (prompt.includes('のの')) return false;
  if (prompt.length < 2 || prompt.length > 20) return false;
  return true;
}

function canAddPrompt(prompt, endingCounts, placeCounts) {
  if ((endingCounts.get(endingKey(prompt)) || 0) >= MAX_ENDING_COUNT) return false;
  for (const place of limitedPlaceHits(prompt)) {
    if ((placeCounts.get(place) || 0) >= MAX_LIMITED_PLACE_COUNT) return false;
  }
  return true;
}

function recordPrompt(prompt, endingCounts, placeCounts) {
  const key = endingKey(prompt);
  endingCounts.set(key, (endingCounts.get(key) || 0) + 1);
  for (const place of limitedPlaceHits(prompt)) {
    placeCounts.set(place, (placeCounts.get(place) || 0) + 1);
  }
}

function tryAdd(candidate, seen, endingCounts, placeCounts, bucket) {
  if (seen.has(candidate) || !isValidPrompt(candidate) || !canAddPrompt(candidate, endingCounts, placeCounts)) {
    return false;
  }
  seen.add(candidate);
  recordPrompt(candidate, endingCounts, placeCounts);
  bucket.push(candidate);
  return true;
}

function* generators() {
  for (const t of times) for (const p of places) yield `${t}の${p}`;
  for (const p of places) for (const q of placeQualities) yield `${p}の${q}`;
  for (const p of places) for (const v of placeVerbs) yield `${p}${v}`;
  for (const p of places) for (const a of animals) yield `${p}の${a}`;
  for (const o of occupations) for (const b of bodyParts) yield `${o}の${b}`;
  for (const w of weather) for (const p of places) yield `${w}の${p}`;
  for (const w of weather) for (const q of placeQualities) yield `${w}の${q}`;
  for (const t of times) for (const q of placeQualities) yield `${t}の${q}`;
  for (const a of adjectives) for (const o of objects) yield `${a}${o}`;
  for (const s of subjects) for (const v of verbs) yield `${s}が${v}`;
  for (const p of places) for (const sfx of placeSuffixes) yield `${p}${sfx}`;
  for (const pre of objectPrefixes) for (const o of objects) yield `${pre}${o}`;
  for (const a of animals) {
    yield `${a}の目`;
    yield `${a}の鳴き声`;
    yield `${a}の足跡`;
    yield `${a}の影`;
    yield `一匹の${a}`;
  }
  for (const c of concepts) yield c;
  for (const act of actions) yield act;
  for (const s of standalone) yield s;
  for (const s of microScenes) yield s;
  for (const s of seasonTouches) yield s;
  for (const p of places) for (const o of objects) yield `${p}の${o}`;
  for (const t of times) for (const o of objects) yield `${t}の${o}`;
  for (const o of occupations) {
    yield `${o}の一日`;
    yield `${o}の夜`;
    yield `${o}の朝`;
    yield `${o}の秘密`;
    yield `${o}の過去`;
  }
  for (const o of objects) for (const l of leftovers) yield `${o}${l}`;
  for (const p of places) for (const m of moments) yield `${p}の${m}`;
  for (const o of occupations) for (const m of moments) yield `${o}の${m}`;
  for (const w of weather) for (const m of moments) yield `${w}の${m}`;
  for (const t of times) for (const m of moments) yield `${t}の${m}`;
}

function* interleaveGenerators() {
  const lists = [];
  for (const p of places) {
    const batch = placeQualities.map((q) => `${p}の${q}`);
    lists.push(batch);
  }
  for (const p of places) {
    const batch = placeVerbs.map((v) => `${p}${v}`);
    lists.push(batch);
  }
  for (const p of places) {
    const batch = animals.map((a) => `${p}の${a}`);
    lists.push(batch);
  }
  for (const o of occupations) {
    const batch = bodyParts.map((b) => `${o}の${b}`);
    lists.push(batch);
  }
  for (const t of times) {
    const batch = placeQualities.map((q) => `${t}の${q}`);
    lists.push(batch);
  }
  for (const w of weather) {
    const batch = placeQualities.map((q) => `${w}の${q}`);
    lists.push(batch);
  }
  for (const t of times) {
    const batch = [];
    for (const p of places) batch.push(`${t}の${p}`);
    lists.push(batch);
  }
  for (const w of weather) {
    const batch = [];
    for (const p of places) batch.push(`${w}の${p}`);
    lists.push(batch);
  }
  for (const a of adjectives) {
    const batch = [];
    for (const o of objects) batch.push(`${a}${o}`);
    lists.push(batch);
  }
  for (const s of subjects) {
    const batch = [];
    for (const v of verbs) batch.push(`${s}が${v}`);
    lists.push(batch);
  }
  for (const p of places) {
    const batch = placeSuffixes.map((sfx) => `${p}${sfx}`);
    lists.push(batch);
  }
  for (const pre of objectPrefixes) {
    const batch = objects.map((o) => `${pre}${o}`);
    lists.push(batch);
  }
  for (const a of animals) {
    lists.push([`${a}の目`, `${a}の鳴き声`, `${a}の足跡`, `${a}の影`, `一匹の${a}`]);
  }
  lists.push([...concepts]);
  lists.push([...actions]);
  lists.push([...standalone]);
  lists.push([...microScenes]);
  lists.push([...seasonTouches]);
  for (const p of places) {
    const batch = objects.map((o) => `${p}の${o}`);
    lists.push(batch);
  }
  for (const t of times) {
    const batch = objects.map((o) => `${t}の${o}`);
    lists.push(batch);
  }
  for (const o of objects) {
    const batch = leftovers.map((l) => `${o}${l}`);
    lists.push(batch);
  }
  for (const o of occupations) {
    lists.push([`${o}の一日`, `${o}の夜`, `${o}の朝`, `${o}の秘密`, `${o}の過去`]);
  }
  for (const p of places) {
    const batch = [];
    for (const m of moments) batch.push(`${p}の${m}`);
    lists.push(batch);
  }
  for (const o of occupations) {
    const batch = [];
    for (const m of moments) batch.push(`${o}の${m}`);
    lists.push(batch);
  }
  for (const w of weather) {
    const batch = moments.map((m) => `${w}の${m}`);
    lists.push(batch);
  }
  for (const t of times) {
    const batch = moments.map((m) => `${t}の${m}`);
    lists.push(batch);
  }

  const indices = lists.map(() => 0);
  while (true) {
    let yielded = false;
    for (let i = 0; i < lists.length; i++) {
      if (indices[i] < lists[i].length) {
        yield lists[i][indices[i]++];
        yielded = true;
      }
    }
    if (!yielded) break;
  }
}

const seen = new Set(ORIGINAL_PROMPTS);
const endingCounts = new Map();
const placeCounts = new Map();
const newPrompts = [];

for (const p of ORIGINAL_PROMPTS) recordPrompt(p, endingCounts, placeCounts);

for (const candidate of interleaveGenerators()) {
  tryAdd(candidate, seen, endingCounts, placeCounts, newPrompts);
  if (newPrompts.length >= 1000) break;
}

if (newPrompts.length < 1000) {
  for (const candidate of generators()) {
    tryAdd(candidate, seen, endingCounts, placeCounts, newPrompts);
    if (newPrompts.length >= 1000) break;
  }
}

if (newPrompts.length < 1000) {
  throw new Error(`Only generated ${newPrompts.length} new prompts`);
}

const allPrompts = [...ORIGINAL_PROMPTS, ...newPrompts];
const lines = allPrompts.map((p) => `    '${p.replace(/'/g, "\\'")}',`).join('\n');
const output = `window.ETUDE_DATA = {
  prompts: [
${lines}
  ],
};
`;

fs.writeFileSync(dataPath, output);

const promptsMdPath = path.join(__dirname, '../etude/prompts.md');
const mdLines = [
  '# Etude お題一覧',
  '',
  `全 ${allPrompts.length} 件。`,
  '',
  ...allPrompts.map((p, i) => `${i + 1}. ${p}`),
  '',
];
fs.writeFileSync(promptsMdPath, mdLines.join('\n'));

console.log(`Original: ${ORIGINAL_PROMPTS.length}`);
console.log(`Added: ${newPrompts.length}`);
console.log(`Total: ${allPrompts.length}`);
console.log('\nTop endings:');
const sorted = [...endingCounts.entries()].sort((a, b) => b[1] - a[1]).slice(0, 15);
for (const [k, v] of sorted) console.log(`  ${v}x ${k}`);
